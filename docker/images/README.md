### Создание образа на основе docker-file
```shell
docker build .
```

```shell
docker build . -t <name_image>:<teg>
```

```shell
docker build . -t my-calendar:4.1.3
```

```shell
docker build . -f <dockerfile_name>
```